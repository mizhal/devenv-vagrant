Claves para acceder a los repositorios:

Es necesario crear un directorio "keys" con la siguiente estructura:

	+ keys
	|-- private
	|	|--- key1
	|	|----key2
	|-- config

El subdirectorio "private" contiene las claves privadas que queramos usar.
El archivo "keys/config" es un archivo de configuracion ssh (~/.ssh/config) que se copia en el directorio $HOME del usuario "vagrant". Ahi se pueden definir todos los hosts y las claves asociadas que sean necesarios.